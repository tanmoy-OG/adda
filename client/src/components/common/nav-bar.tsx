import { FC } from "react";

const NavBar: FC = () => {
  return (
    <div className="navbar bg-base-100 hover:cursor-default">
      <div className="text-center font-semibold text-xl mx-auto">Addaa</div>
    </div>
  );
};

export default NavBar;
